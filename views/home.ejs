<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <link rel="stylesheet" href="/style.css" />
</head>

<body>
    <%- include('../public/partials/header.ejs') %>
        <% if (blogs.length < 1) { %>
            <h1 class="no-blog-title">
                You don't have any blogs yet. Please visit <br />the home page to create a
                new blog.
            </h1>
            <% } else {%>
                <h1 class="title">Your Blogs list</h1>
                <% } %>

                    <div class="overflow-div">
                        <div class="home-container">
                            <% if (locals.blogs) { %>
                                <% for( let index=0; index < blogs.length; index++ ) { %>
                                    <div class="blog-card">
                                        <span class="blog-number">
                                            <%= index+ 1 %>
                                        </span>
                                        <h4>
                                            <%= blogs[index].title %>
                                        </h4>
                                        <p>
                                            <%= blogs[index].description?.slice(0,300) %>
                                                <% if (blogs[index].description?.length> 299) { %> ...
                                                    <a class="view-btn" href="/blogDetails/<%= blogs[index].id %>">
                                                        View Blog</a>
                                                    <% } %>
                                        </p>
                                        <div class="btn-wrapper">
                                            <form action="/delete/<%= blogs[index].id %>" method="post">
                                                <button class="delete-btn">Delete</button>
                                            </form>
                                            <a class="edit-btn" href="/edit/<%=  blogs[index].id %>">edit</a>
                                        </div>
                                    </div>
                                    <% } %>
                                        <% } else { %>
                                            <h1>NO Blogs here</h1>

                                            <% } %>
                        </div>
                    </div>

                    <%- include('../public/partials/footer.ejs') %>
</body>

</html>